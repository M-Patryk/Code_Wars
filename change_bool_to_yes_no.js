//Change bool values, so when variable == True, it prints "Yes" instead, same for false, -> "No"

function changeBool(bool) {
	if (bool == true) {
		return 'Yes';
	} else {
		return 'No';
	}
}

changeBool();
